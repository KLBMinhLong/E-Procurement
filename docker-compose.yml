name: eprocure

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

x-common-env: &common-env
  TZ: ${TZ:-Asia/Ho_Chi_Minh}

services:
  postgres:
    image: postgres:15-alpine
    container_name: eprocure-postgres
    restart: unless-stopped
    environment:
      <<: *common-env
      POSTGRES_USER: ${POSTGRES_ADMIN_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASS:-postgres_dev_pass}
      POSTGRES_DB: postgres
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infra/postgres/init/00-init-databases.sql:/docker-entrypoint-initdb.d/00-init-databases.sql:ro
    networks:
      - eprocure-net
    cpus: 0.50
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    logging: *default-logging

  redis:
    image: redis:7-alpine
    container_name: eprocure-redis
    restart: unless-stopped
    command:
      - redis-server
      - --maxmemory
      - 100mb
      - --maxmemory-policy
      - allkeys-lru
      - --save
      - ""
      - --appendonly
      - "no"
    environment:
      <<: *common-env
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    networks:
      - eprocure-net
    cpus: 0.10
    mem_limit: 128m
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 128M
        reservations:
          cpus: "0.05"
          memory: 64M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    logging: *default-logging

  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: eprocure-kafka
    restart: unless-stopped
    environment:
      <<: *common-env
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_NODE_ID: 1
      CLUSTER_ID: ${KAFKA_CLUSTER_ID:-8F4Qf35tQ2e8rE9rQ9k8wA}
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:9092,EXTERNAL://0.0.0.0:19092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:9092,EXTERNAL://localhost:${KAFKA_EXTERNAL_PORT:-19092}
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_LOG_DIRS: /var/lib/kafka/data
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_HEAP_OPTS: "-Xms128m -Xmx384m"
    ports:
      - "${KAFKA_EXTERNAL_PORT:-19092}:19092"
    networks:
      - eprocure-net
    cpus: 0.50
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 45s
    logging: *default-logging

  kafka-init:
    image: confluentinc/cp-kafka:7.6.1
    container_name: eprocure-kafka-init
    depends_on:
      kafka:
        condition: service_healthy
    environment:
      <<: *common-env
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KAFKA_HEAP_OPTS: "-Xms128m -Xmx384m"
    volumes:
      - ./infra/kafka/create-topics.sh:/scripts/create-topics.sh:ro
    command: ["bash", "/scripts/create-topics.sh"]
    networks:
      - eprocure-net
    cpus: 0.50
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
    restart: "no"
    logging: *default-logging

  keycloak:
    build:
      context: .
      dockerfile: infra/keycloak/Dockerfile
    container_name: eprocure-keycloak
    restart: unless-stopped
    command: ["start-dev", "--import-realm"]
    environment:
      <<: *common-env
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN_USER:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASS:-admin_dev_pass}
      KC_DB: postgres
      KC_DB_URL_HOST: postgres
      KC_DB_URL_PORT: 5432
      KC_DB_URL_DATABASE: db_iam
      KC_DB_SCHEMA: iam_keycloak
      KC_DB_USERNAME: ${IAM_DB_USER:-iam_user}
      KC_DB_PASSWORD: ${IAM_DB_PASS:-iam_pass_dev}
      KC_HEALTH_ENABLED: "true"
      KC_HTTP_ENABLED: "true"
      KC_HTTP_PORT: 8080
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET:?Set KEYCLOAK_CLIENT_SECRET in .env}
      IAM_PROVIDER_BASE_URL: ${IAM_PROVIDER_BASE_URL:?Set IAM_PROVIDER_BASE_URL in .env}
      IAM_PROVIDER_TIMEOUT_SECONDS: ${IAM_PROVIDER_TIMEOUT_SECONDS:-3}
      IAM_INTERNAL_API_KEY: ${IAM_INTERNAL_API_KEY:?Set IAM_INTERNAL_API_KEY in .env}
    ports:
      - "${KEYCLOAK_PORT:-18080}:8080"
    volumes:
      - ./infra/keycloak/realm-eprocure.json:/opt/keycloak/data/import/realm-eprocure.json:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - eprocure-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    cpus: 0.25
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 512M
        reservations:
          cpus: "0.10"
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "timeout 5 bash -c '</dev/tcp/localhost/8080' || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 60s
    logging: *default-logging

  iam-service:
    build:
      context: .
      dockerfile: services/iam-service/Dockerfile
    container_name: eprocure-iam-service
    restart: unless-stopped
    environment:
      <<: *common-env
      SERVICE_NAME: iam-service
      SERVER_PORT: 8081
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      OTEL_SERVICE_NAME: iam-service
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://tempo:4317}
      OTEL_EXPORTER_OTLP_PROTOCOL: grpc
      # OTEL_TRACES_EXPORTER: otlp (if you want to export traces to tempo)
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_PROPAGATORS: tracecontext,baggage
      IAM_DB_HOST: postgres
      IAM_DB_PORT: 5432
      IAM_DB_NAME: db_iam
      IAM_DB_SCHEMA: iam
      IAM_DB_USER: ${IAM_DB_USER:-iam_user}
      IAM_DB_PASS: ${IAM_DB_PASS:-iam_pass_dev}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASS: ${REDIS_PASS:-}
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-eprocure}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID:-eprocure-iam}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET}
      IAM_INTERNAL_API_KEY: ${IAM_INTERNAL_API_KEY}
      SESSION_TTL_HOURS: ${SESSION_TTL_HOURS:-8}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      COOKIE_SAMESITE: ${COOKIE_SAMESITE:-Strict}
      ENCRYPTION_ENABLED: ${ENCRYPTION_ENABLED:-false}
      RSA_KEY_VERSION: ${RSA_KEY_VERSION:-v2025-01}
      RSA_PUBLIC_KEY: ${RSA_PUBLIC_KEY:-}
      RSA_PRIVATE_KEY: ${RSA_PRIVATE_KEY:-}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      IAM_PASSWORD_RESET_DELIVERY_MODE: ${IAM_PASSWORD_RESET_DELIVERY_MODE:-kafka}
      IAM_PASSWORD_RESET_TOPIC: ${IAM_PASSWORD_RESET_TOPIC:-notification.email.send}
      IAM_PASSWORD_RESET_LANGUAGE: ${IAM_PASSWORD_RESET_LANGUAGE:-vi}
      PASSWORD_RESET_FRONTEND_URL: ${PASSWORD_RESET_FRONTEND_URL:-http://localhost:4200/reset-password}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:8081/api/v1/auth/oauth/google/callback}
    ports:
      - "${IAM_SERVICE_PORT:-8081}:8081"
    networks:
      - eprocure-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      keycloak:
        condition: service_healthy
    cpus: 0.25
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 512M
        reservations:
          cpus: "0.10"
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8081/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 75s
    logging: *default-logging

  pr-service:
    build:
      context: .
      dockerfile: services/purchase-request-service/Dockerfile
    container_name: eprocure-pr-service
    restart: unless-stopped
    environment:
      <<: *common-env
      SERVICE_NAME: purchase-request-service
      SERVER_PORT: 8082
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      OTEL_SERVICE_NAME: purchase-request-service
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://tempo:4317}
      OTEL_EXPORTER_OTLP_PROTOCOL: grpc
      # OTEL_TRACES_EXPORTER: otlp
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_PROPAGATORS: tracecontext,baggage
      PR_DB_HOST: postgres
      PR_DB_PORT: 5432
      PR_DB_NAME: db_procurement
      PR_DB_SCHEMA: pr
      PR_DB_USER: ${PR_DB_USER:-pr_user}
      PR_DB_PASS: ${PR_DB_PASS:-pr_pass_dev}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASS: ${REDIS_PASS:-}
      PR_NUMBER_PREFIX: ${PR_NUMBER_PREFIX:-PR}
      PR_ATTACHMENT_MAX_SIZE_MB: ${PR_ATTACHMENT_MAX_SIZE_MB:-20}
      PR_EMERGENCY_MONTHLY_LIMIT: ${PR_EMERGENCY_MONTHLY_LIMIT:-3}
      PR_INTEGRATION_FALLBACK_ENABLED: ${PR_INTEGRATION_FALLBACK_ENABLED:-false}
      PR_FINANCE_INTEGRATION_ENABLED: ${PR_FINANCE_INTEGRATION_ENABLED:-true}
      FINANCE_SERVICE_URL: http://finance-service:8084
      FINANCE_INTERNAL_API_KEY: ${FINANCE_INTERNAL_API_KEY:-change-me-internal-api-key}
      PR_DEFAULT_GL_ACCOUNT_CODE: ${PR_DEFAULT_GL_ACCOUNT_CODE:-6002}
      PR_INTERNAL_API_KEY: ${PR_INTERNAL_API_KEY:-change-me-internal-api-key}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    ports:
      - "${PR_SERVICE_PORT:-8082}:8082"
    networks:
      - eprocure-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      finance-service:
        condition: service_healthy
    cpus: 0.25
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 512M
        reservations:
          cpus: "0.10"
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8082/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 75s
    logging: *default-logging

  finance-service:
    build:
      context: .
      dockerfile: services/finance-service/Dockerfile
    container_name: eprocure-finance-service
    restart: unless-stopped
    environment:
      <<: *common-env
      SERVICE_NAME: finance-service
      SERVER_PORT: 8084
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      OTEL_SERVICE_NAME: finance-service
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://tempo:4317}
      OTEL_EXPORTER_OTLP_PROTOCOL: grpc
      # OTEL_TRACES_EXPORTER: otlp
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_PROPAGATORS: tracecontext,baggage
      FINANCE_DB_HOST: postgres
      FINANCE_DB_PORT: 5432
      FINANCE_DB_NAME: db_finance
      FINANCE_DB_SCHEMA: finance
      FINANCE_DB_USER: ${FINANCE_DB_USER:-finance_user}
      FINANCE_DB_PASS: ${FINANCE_DB_PASS:-finance_pass_dev}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASS: ${REDIS_PASS:-}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      FINANCE_INTERNAL_API_KEY: ${FINANCE_INTERNAL_API_KEY:-change-me-internal-api-key}
      FINANCE_KAFKA_ENABLED: ${FINANCE_KAFKA_ENABLED:-true}
      FINANCE_KAFKA_AUTO_STARTUP: ${FINANCE_KAFKA_AUTO_STARTUP:-true}
      FINANCE_DEFAULT_GL_ACCOUNT_CODE: ${FINANCE_DEFAULT_GL_ACCOUNT_CODE:-6002}
    ports:
      - "${FINANCE_SERVICE_PORT:-8084}:8084"
    networks:
      - eprocure-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    cpus: 0.25
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 512M
        reservations:
          cpus: "0.10"
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8084/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 120s
    logging: *default-logging

  vendor-service:
    build:
      context: .
      dockerfile: services/vendor-service/Dockerfile
    container_name: eprocure-vendor-service
    restart: unless-stopped
    environment:
      <<: *common-env
      SERVICE_NAME: vendor-service
      SERVER_PORT: 8086
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      OTEL_SERVICE_NAME: vendor-service
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://tempo:4317}
      OTEL_EXPORTER_OTLP_PROTOCOL: grpc
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_PROPAGATORS: tracecontext,baggage
      VENDOR_DB_HOST: postgres
      VENDOR_DB_PORT: 5432
      VENDOR_DB_NAME: db_vendor
      VENDOR_DB_SCHEMA: vendor
      VENDOR_DB_USER: ${VENDOR_DB_USER:-vendor_user}
      VENDOR_DB_PASS: ${VENDOR_DB_PASS:-vendor_pass_dev}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASS: ${REDIS_PASS:-}
      VENDOR_INTERNAL_API_KEY: ${VENDOR_INTERNAL_API_KEY:-change-me-internal-api-key}
      PR_SERVICE_URL: http://pr-service:8082
      PR_INTERNAL_API_KEY: ${PR_INTERNAL_API_KEY:-change-me-internal-api-key}
    ports:
      - "${VENDOR_SERVICE_PORT:-8086}:8086"
    networks:
      - eprocure-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      pr-service:
        condition: service_healthy
    cpus: 0.25
    mem_limit: 256m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8086/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 75s
    logging: *default-logging

  notification-service:
    build:
      context: .
      dockerfile: services/notification-service/Dockerfile
    container_name: eprocure-notification-service
    restart: unless-stopped
    environment:
      <<: *common-env
      SERVICE_NAME: notification-service
      SERVER_PORT: 8088
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      OTEL_SERVICE_NAME: notification-service
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://tempo:4317}
      OTEL_EXPORTER_OTLP_PROTOCOL: grpc
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_PROPAGATORS: tracecontext,baggage
      NOTIFICATION_DB_HOST: postgres
      NOTIFICATION_DB_PORT: 5432
      NOTIFICATION_DB_NAME: db_notification
      NOTIFICATION_DB_SCHEMA: notification
      NOTIFICATION_DB_USER: ${NOTIFICATION_DB_USER:-notification_user}
      NOTIFICATION_DB_PASS: ${NOTIFICATION_DB_PASS:-notification_pass_dev}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASS: ${REDIS_PASS:-}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      NOTIFICATION_INTERNAL_API_KEY: ${NOTIFICATION_INTERNAL_API_KEY:-change-me-internal-api-key}
      NOTIFICATION_KAFKA_ENABLED: ${NOTIFICATION_KAFKA_ENABLED:-true}
      NOTIFICATION_KAFKA_AUTO_STARTUP: ${NOTIFICATION_KAFKA_AUTO_STARTUP:-true}
      NOTIFICATION_BUDGET_ALERT_RECIPIENT_IDS: ${NOTIFICATION_BUDGET_ALERT_RECIPIENT_IDS:-10000000-0000-4000-8000-000000000001}
      NOTIFICATION_EMAIL_DISPATCH_ENABLED: ${NOTIFICATION_EMAIL_DISPATCH_ENABLED:-true}
      NOTIFICATION_EMAIL_PROVIDER: ${NOTIFICATION_EMAIL_PROVIDER:-logging}
      NOTIFICATION_EMAIL_BATCH_SIZE: ${NOTIFICATION_EMAIL_BATCH_SIZE:-25}
      NOTIFICATION_EMAIL_RETRY_FIXED_DELAY_MS: ${NOTIFICATION_EMAIL_RETRY_FIXED_DELAY_MS:-60000}
      EMAIL_RETRY_MAX: ${EMAIL_RETRY_MAX:-3}
      EMAIL_RETRY_DELAY_SECONDS: ${EMAIL_RETRY_DELAY_SECONDS:-30}
      EMAIL_FROM_NAME: ${EMAIL_FROM_NAME:-eProcure Dev}
      EMAIL_FROM_ADDRESS: ${EMAIL_FROM_ADDRESS:-dev@eprocure.local}
      BREVO_API_URL: ${BREVO_API_URL:-https://api.brevo.com/v3/smtp/email}
      BREVO_API_KEY: ${BREVO_API_KEY:-}
    ports:
      - "${NOTIFICATION_SERVICE_PORT:-8088}:8088"
    networks:
      - eprocure-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    cpus: 0.25
    mem_limit: 256m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8088/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 300s
    logging: *default-logging

  approval-service:
    build:
      context: .
      dockerfile: services/approval-service/Dockerfile
    container_name: eprocure-approval-service
    restart: unless-stopped
    environment:
      <<: *common-env
      SERVICE_NAME: approval-service
      SERVER_PORT: 8083
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      OTEL_SERVICE_NAME: approval-service
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://tempo:4317}
      OTEL_EXPORTER_OTLP_PROTOCOL: grpc
      # OTEL_TRACES_EXPORTER: otlp
      OTEL_TRACES_EXPORTER: none
      OTEL_METRICS_EXPORTER: none
      OTEL_LOGS_EXPORTER: none
      OTEL_PROPAGATORS: tracecontext,baggage
      APPROVAL_DB_HOST: postgres
      APPROVAL_DB_PORT: 5432
      APPROVAL_DB_NAME: db_procurement
      APPROVAL_DB_SCHEMA: approval
      APPROVAL_DB_USER: ${APPROVAL_DB_USER:-approval_user}
      APPROVAL_DB_PASS: ${APPROVAL_DB_PASS:-approval_pass_dev}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASS: ${REDIS_PASS:-}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      APPROVAL_INTEGRATION_FALLBACK_ENABLED: ${APPROVAL_INTEGRATION_FALLBACK_ENABLED:-false}
      APPROVAL_INTERNAL_API_KEY: ${APPROVAL_INTERNAL_API_KEY:-change-me-internal-api-key}
      IAM_SERVICE_URL: http://iam-service:8081
      IAM_INTERNAL_API_KEY: ${IAM_INTERNAL_API_KEY:-change-me-internal-api-key}
      PR_SERVICE_URL: http://pr-service:8082
      PR_INTERNAL_API_KEY: ${PR_INTERNAL_API_KEY:-change-me-internal-api-key}
    ports:
      - "${APPROVAL_SERVICE_PORT:-8083}:8083"
    networks:
      - eprocure-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    cpus: 0.50
    mem_limit: 768m
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 768M
        reservations:
          cpus: "0.10"
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8083/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 90s
    logging: *default-logging

  nginx-gateway:
    image: nginx:1.25-alpine
    container_name: eprocure-nginx-gateway
    restart: unless-stopped
    environment:
      <<: *common-env
      IAM_SERVICE_URL: ${IAM_SERVICE_URL:-http://iam-service:8081}
      PR_SERVICE_URL: ${PR_SERVICE_URL:-http://pr-service:8082}
      PR_INTERNAL_API_KEY: ${PR_INTERNAL_API_KEY:-change-me-internal-api-key}
      APPROVAL_SERVICE_URL: ${APPROVAL_SERVICE_URL:-http://approval-service:8083}
      FINANCE_SERVICE_URL: ${FINANCE_SERVICE_URL:-http://finance-service:8084}
      INVENTORY_SERVICE_URL: ${INVENTORY_SERVICE_URL:-http://inventory-service:8085}
      VENDOR_SERVICE_URL: ${VENDOR_SERVICE_URL:-http://vendor-service:8086}
      ANALYTICS_SERVICE_URL: ${ANALYTICS_SERVICE_URL:-http://analytics-service:8087}
      NOTIFICATION_SERVICE_URL: ${NOTIFICATION_SERVICE_URL:-http://notification-service:8088}
      ADMIN_SERVICE_URL: ${ADMIN_SERVICE_URL:-http://admin-service:8089}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:4200}
      NGINX_INTERNAL_API_KEY: ${NGINX_INTERNAL_API_KEY:-change-me-internal-api-key}
    ports:
      - "${GATEWAY_PORT:-8080}:80"
    volumes:
      - ./infra/nginx/templates/default.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./infra/nginx/templates/proxy-common.template:/etc/nginx/templates/proxy-common.template:ro
      - ./infra/nginx/templates/proxy-protected.template:/etc/nginx/templates/proxy-protected.template:ro
    networks:
      - eprocure-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    cpus: 0.10
    mem_limit: 64m
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 64M
        reservations:
          cpus: "0.05"
          memory: 32M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1/health"]
      interval: 15s
      timeout: 5s
      retries: 5
    logging: *default-logging

  prometheus:
    image: prom/prometheus:v2.52.0
    container_name: eprocure-prometheus
    restart: unless-stopped
    profiles: ["monitoring"]
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=7d
    environment:
      <<: *common-env
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    volumes:
      - ./infra/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - eprocure-net
    cpus: 0.10
    mem_limit: 128m
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 128M
    logging: *default-logging

  grafana:
    image: grafana/grafana:10.4.2
    container_name: eprocure-grafana
    restart: unless-stopped
    profiles: ["monitoring"]
    environment:
      <<: *common-env
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASS:-admin_dev_pass}
      GF_USERS_ALLOW_SIGN_UP: "false"
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./infra/grafana/provisioning:/etc/grafana/provisioning:ro
    networks:
      - eprocure-net
    depends_on:
      - prometheus
      - loki
      - tempo
    cpus: 0.10
    mem_limit: 128m
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 128M
    logging: *default-logging

  loki:
    image: grafana/loki:2.9.8
    container_name: eprocure-loki
    restart: unless-stopped
    profiles: ["monitoring"]
    command: ["-config.file=/etc/loki/loki-config.yml"]
    environment:
      <<: *common-env
    ports:
      - "${LOKI_PORT:-3100}:3100"
    volumes:
      - ./infra/loki/loki-config.yml:/etc/loki/loki-config.yml:ro
      - loki-data:/loki
    networks:
      - eprocure-net
    cpus: 0.10
    mem_limit: 128m
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 128M
    logging: *default-logging

  tempo:
    image: grafana/tempo:2.4.1
    container_name: eprocure-tempo
    restart: unless-stopped
    profiles: ["monitoring"]
    command: ["-config.file=/etc/tempo/tempo.yml"]
    environment:
      <<: *common-env
    ports:
      - "${TEMPO_PORT:-3200}:3200"
      - "${TEMPO_OTLP_GRPC_PORT:-4317}:4317"
      - "${TEMPO_OTLP_HTTP_PORT:-4318}:4318"
    volumes:
      - ./infra/tempo/tempo.yml:/etc/tempo/tempo.yml:ro
      - tempo-data:/tmp/tempo
    networks:
      - eprocure-net
    cpus: 0.25
    mem_limit: 512m
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 512M
        reservations:
          cpus: "0.10"
          memory: 128M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3200/ready"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    logging: *default-logging

networks:
  eprocure-net:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  prometheus-data:
  grafana-data:
  loki-data:
  tempo-data:
