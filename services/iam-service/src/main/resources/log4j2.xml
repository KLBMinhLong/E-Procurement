<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <Properties>
        <Property name="SERVICE_NAME">iam-service</Property>
        <Property name="LOG_DIR">./logs</Property>
        <Property name="CONSOLE_PATTERN">%highlight{%d{yyyy-MM-dd HH:mm:ss.SSS}}{FATAL=red, ERROR=red, WARN=yellow, INFO=white, DEBUG=cyan} %highlight{[%-5level]}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=green, DEBUG=cyan} [%style{%X{traceId}}{blue}/%style{%X{spanId}}{blue}] [${SERVICE_NAME}] [%style{%t}{bright,black}] %cyan{%-40.40c{1.}} - %msg%n</Property>
        <Property name="FILE_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%X{traceId}/%X{spanId}] [${SERVICE_NAME}] [%t] %-40.40c{1.} - %msg%n</Property>
    </Properties>

    <Appenders>
        <!-- Console with colors -->
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_PATTERN}" disableAnsi="false"/>
        </Console>

        <!-- Application log file -->
        <RollingFile name="FILE"
                     fileName="${LOG_DIR}/application.log"
                     filePattern="${LOG_DIR}/application-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${FILE_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="50MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- Audit log — append-only, separate file -->
        <RollingFile name="AUDIT"
                     fileName="${LOG_DIR}/audit.log"
                     filePattern="${LOG_DIR}/audit-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${FILE_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <DefaultRolloverStrategy max="1825"/> <!-- 5 years -->
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Application -->
        <Logger name="com.eprocure" level="${env:LOG_LEVEL_APP:-DEBUG}" additivity="false">
            <AppenderRef ref="CONSOLE"/>
            <AppenderRef ref="FILE"/>
        </Logger>

        <!-- Audit loggers -->
        <Logger name="AUDIT" level="INFO" additivity="false">
            <AppenderRef ref="AUDIT"/>
        </Logger>
        <Logger name="com.eprocure.audit" level="INFO" additivity="false">
            <AppenderRef ref="AUDIT"/>
        </Logger>

        <!-- Framework logging limits -->
        <Logger name="org.springframework"          level="WARN"/>
        <Logger name="org.mybatis"                  level="WARN"/>
        <Logger name="com.zaxxer.hikari"            level="WARN"/>
        <Logger name="org.apache.kafka"             level="WARN"/>
        <Logger name="io.lettuce"                   level="WARN"/>

        <Root level="WARN">
            <AppenderRef ref="CONSOLE"/>
            <AppenderRef ref="FILE"/>
        </Root>
    </Loggers>

</Configuration>
